[package.json](https://github.com/user-attachments/files/31870456/package.json)
[README.txt](https://github.com/user-attachments/files/31870459/README.txt)[server.js](https://github.com/user-attachments/files/31870460/server.js)const express=require('express'),http=require('http'),{Server}=require('socket.io'),Database=require('better-sqlite3'),path=require('path'),crypto=require('crypto');
const app=express(),server=http.createServer(app),io=new Server(server),PORT=process.env.PORT||3000;
const ADMIN_USER='عمار123456789',ADMIN_PASS='2014';
const db=new Database(path.join(__dirname,'platform.db'));db.pragma('journal_mode=WAL');
db.exec(`CREATE TABLE IF NOT EXISTS students(code TEXT PRIMARY KEY,name TEXT NOT NULL DEFAULT '');CREATE TABLE IF NOT EXISTS videos(id INTEGER PRIMARY KEY AUTOINCREMENT,code TEXT NOT NULL,title TEXT NOT NULL,url TEXT NOT NULL);CREATE TABLE IF NOT EXISTS questions(id INTEGER PRIMARY KEY AUTOINCREMENT,code TEXT NOT NULL,question TEXT NOT NULL,option1 TEXT NOT NULL,option2 TEXT NOT NULL,option3 TEXT NOT NULL,option4 TEXT NOT NULL,answer INTEGER NOT NULL);`);
if(db.prepare('SELECT COUNT(*) n FROM students').get().n<100){const ins=db.prepare('INSERT OR IGNORE INTO students(code,name) VALUES(?,?)');const tx=db.transaction(()=>{for(let i=1;i<=100;i++)ins.run(`TECH${String(i).padStart(3,'0')}`,'')});tx()}
const getStudent=code=>{const s=db.prepare('SELECT code,name FROM students WHERE code=?').get(code);if(!s)return null;s.videos=db.prepare('SELECT id,title,url FROM videos WHERE code=? ORDER BY id DESC').all(code);s.questions=db.prepare('SELECT id,question,option1,option2,option3,option4,answer FROM questions WHERE code=? ORDER BY id DESC').all(code);return s};
const students=()=>db.prepare('SELECT code,name FROM students ORDER BY code').all();const validUrl=u=>{try{return ['http:','https:'].includes(new URL(u).protocol)}catch{return false}};const tokens=new Set(),isAdmin=t=>tokens.has(t);
app.use(express.static(path.join(__dirname,'public')));app.get('/health',(q,r)=>r.json({ok:true}));
io.on('connection',s=>{
 s.on('adminLogin',({user,pass},cb)=>{if(user===ADMIN_USER&&pass===ADMIN_PASS){const t=crypto.randomBytes(32).toString('hex');tokens.add(t);cb({ok:true,token:t,students:students()})}else cb({ok:false,error:'بيانات المدير غير صحيحة'})});
 s.on('getStudents',({token},cb)=>isAdmin(token)?cb({ok:true,students:students()}):cb({ok:false,error:'غير مصرح'}));
 s.on('getStudent',({token,code},cb)=>{if(!isAdmin(token))return cb({ok:false,error:'غير مصرح'});const x=getStudent(String(code||'').toUpperCase());cb(x?{ok:true,student:x}:{ok:false,error:'الكود غير موجود'})});
 s.on('studentLogin',({code},cb)=>{code=String(code||'').trim().toUpperCase();const x=getStudent(code);if(!x)return cb({ok:false,error:'الكود غير صحيح'});s.join(code);cb({ok:true,student:x})});
 s.on('saveStudent',({token,code,name},cb)=>{if(!isAdmin(token))return cb({ok:false,error:'غير مصرح'});code=String(code||'').toUpperCase();if(!getStudent(code))return cb({ok:false,error:'الكود غير موجود'});db.prepare('UPDATE students SET name=? WHERE code=?').run(String(name||'').trim(),code);io.to(code).emit('contentUpdated',getStudent(code));cb({ok:true})});
 s.on('addVideo',({token,code,title,url},cb)=>{if(!isAdmin(token))return cb({ok:false,error:'غير مصرح'});code=String(code||'').toUpperCase();title=String(title||'').trim();url=String(url||'').trim();if(!getStudent(code))return cb({ok:false,error:'الكود غير موجود'});if(!title||!validUrl(url))return cb({ok:false,error:'اكتب عنوانًا ورابط http/https صحيحًا'});db.prepare('INSERT INTO videos(code,title,url) VALUES(?,?,?)').run(code,title,url);io.to(code).emit('contentUpdated',getStudent(code));cb({ok:true})});
 s.on('deleteVideo',({token,id,code},cb)=>{if(!isAdmin(token))return cb({ok:false,error:'غير مصرح'});code=String(code||'').toUpperCase();db.prepare('DELETE FROM videos WHERE id=? AND code=?').run(Number(id),code);io.to(code).emit('contentUpdated',getStudent(code));cb({ok:true})});
 s.on('addQuestion',({token,code,question,options,answer},cb)=>{if(!isAdmin(token))return cb({ok:false,error:'غير مصرح'});code=String(code||'').toUpperCase();options=Array.isArray(options)?options.map(x=>String(x||'').trim()):[];question=String(question||'').trim();answer=Number(answer);if(!getStudent(code)||!question||options.length!==4||options.some(x=>!x)||!Number.isInteger(answer)||answer<0||answer>3)return cb({ok:false,error:'أكمل السؤال والاختيارات الأربعة وحدد الإجابة الصحيحة'});db.prepare('INSERT INTO questions(code,question,option1,option2,option3,option4,answer) VALUES(?,?,?,?,?,?,?)').run(code,question,...options,answer);io.to(code).emit('contentUpdated',getStudent(code));cb({ok:true})});
 s.on('deleteQuestion',({token,id,code},cb)=>{if(!isAdmin(token))return cb({ok:false,error:'غير مصرح'});code=String(code||'').toUpperCase();db.prepare('DELETE FROM questions WHERE id=? AND code=?').run(Number(id),code);io.to(code).emit('contentUpdated',getStudent(code));cb({ok:true})});
});
app.use((req,res,next)=>{if(req.method==='GET'&&!req.path.startsWith('/socket.io'))res.sendFile(path.join(__dirname,'public','index.html'));else next()});server.listen(PORT,()=>console.log('Technology Platform on '+PORT));
